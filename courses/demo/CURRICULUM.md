# demo

> Generated by the Praxis Generator. Each row is one Module.

| # | Module | `slug` | Core principle | Status |
| - | ------ | ------ | -------------- | ------ |
| 1 | The Base Case | `base-case` | Every recursive function needs a stopping condition or it runs forever. | 🟢 |
| 2 | The Recursive Step | `recursive-step` | The recursive step solves the problem by reducing it to a smaller version of itself. | 🟢 |
| 3 | The Call Stack | `call-stack` | The call stack is the runtime's bookkeeping of every active recursive call — each call pauses until the one it spawned returns. | 🟢 |
