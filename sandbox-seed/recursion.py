"""Your recursion reps. Make the tests in test_recursion.py pass.

Run from the sandbox/ folder:  python -m unittest -v
Write these RECURSIVELY (a function that calls itself) — no loops.
"""


def countdown(n):
    """Module 1 — base-case.

    Return a list from n down to 1, then 0 as the base case.
    countdown(3) -> [3, 2, 1, 0].  countdown(0) -> [0].
    """
    raise NotImplementedError("write countdown recursively — what stops it?")


def sum_to(n):
    """Module 2 — recursive-step.

    Return 0 + 1 + ... + n using recursion (no loops).
    sum_to(4) -> 10.  sum_to(0) -> 0.
    """
    raise NotImplementedError("write sum_to recursively — how does n shrink?")
