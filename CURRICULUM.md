# Defender Server Architecture

> Generated by the Praxis Generator. Each row is one Module.

| # | Module | `slug` | Core principle | Status |
| - | ------ | ------ | -------------- | ------ |
| 1 | The Two-Process Model | `two-process-model` | The hub holds no desired state — the control plane owns it, and the hub converges to it by polling. | 🟢 |
| 2 | Hub Loops & the WireGuard Data Plane | `hub-loops-data-plane` | The hub converges the live WireGuard device to desired state via four independent concurrent loops, and relays spoke-to-spoke traffic as a trusted plaintext intermediary. | 🟢 |
| 3 | Three Cryptographic Layers | `cryptographic-layers` | Three independent cryptographic layers each protect something different — transport, API identity, and VPN traffic — and must not be conflated. | 🟢 |
| 4 | Endpoints, Data Model & ACL Pipeline | `endpoints-data-model-acl` | Three trust tiers gate every endpoint; the store ensures durable spoke identity through key rotation; ACLs are compiled centrally from tags and applied atomically on the hub. | 🟢 |
